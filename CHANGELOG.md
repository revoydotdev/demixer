# Changelog

_Generated by `revoy changelog` from conventional-commit history — do not hand-edit._

## v0.1.0 — 2026-07-05

### Features
- output file tree reflecting the real run output (`9e14aae`)
- align RunOptions.chords default to btc (matches CLI) (`abbfc15`)
- flip default chord engine to btc (accuracy-first; autochord via --chords autochord) (`74d91b6`)
- --midi-hint trusts a ground-truth MIDI for tempo and meter (`954953f`)
- FL Studio .flp export and piano-roll Python scripts (`2796f09`)

### Fixes
- reject beats_per_bar < 2, fall back to 4/4 and demote reliability (`5d2cece`)

### Performance
- --detect-loops runs Demucs on one period and tiles the stems (`d8022b6`)
- --skip-projects-if-single-stem collapses one-stem runs (`a9639d8`)
- gate PNG and MP3 renders behind --score-renders, default to PDF + MSCZ (`ad2fb67`)
- --compact-archive drops loose stems from .demixer when a dawproject is present (`9133fde`)
- default stems to PCM_24, expose --stem-format, store precompressed payloads (`d9c875c`)
- skip silent stems in write_stems (`dec8ee3`)

### Docs
- drop shipped GUI interaction todo (picker/progress/output tree) (`3b9b7c2`)
- add status/tech-stack badge row (`787db3f`)

### Chores
- v0.1.0 (`12540a1`)
- add .gitattributes (`0ad01ab`)
- drop shipped BTC-default todo from phase block; add ROADMAP.md (`3f55b47`)

## v0.0.1 — 2026-05-26

### Features
- accuracy harnesses and Isophonics ground-truth matrix (`f4e1701`)
- PySide6 desktop shell and pipeline runner (`3e59062`)
- end-to-end "demixer process" pipeline with per-stage skips (`1473c7f`)
- pack stems, MIDI, analysis, score, and projects into .demixer (`a164a21`)
- DAW round-trip exports for Reaper, DAWproject, and drag-in (`92db5a6`)
- MIDI quantization, MusicXML engraving, Verovio/MuseScore rendering (`3054cc2`)
- pitched (basic-pitch/MT3) and drum (spectral/ADTOF) to MIDI (`3570cae`)
- parametric harmony descriptors and reharmonization (`c63c0c5`)
- chord recognition via autochord and BTC large-vocabulary (`13c8889`)
- confidence-gated tempo/beat tracking and key estimation (`70586fc`)
- Demucs v4 stems with optional BS-RoFormer vocals (`52d8236`)
- isolated-venv subprocess worker protocol for SOTA engines (`4b39f3b`)
- audio ingest with EBU R128 loudness normalization (`6f22c46`)

### Docs
- project README (`f6a16e7`)

### Chores
- scaffold project packaging, tooling, and AGPL-3.0 license (`71bb0f6`)
